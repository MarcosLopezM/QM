name: Compile LaTeX projects

on:
  push:
    paths:
      - "Tareas/T1/**"
      - "Tareas/T2/**"

jobs:
  build-tarea-1:
    uses: MarcosLopezM/latex-workflow/.github/workflows/build-pdf.yml@main
    with:
      relative_path: Tareas/T1
      out_name: "Tarea1"
      email_to: "marcoslm@ciencias.unam.mx"
    secrets: inherit

  build-tarea-2:
    uses: MarcosLopezM/latex-workflow/.github/workflows/build-pdf.yml@main
    with:
      relative_path: Tareas/T2
      out_name: "Tarea2"
      send_it: true
      email_to: "marcoslm@ciencias.unam.mx"
    secrets: inherit
